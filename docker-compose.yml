version: "3.8"

services:
  web:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      - FLASK_DEBUG=1
    volumes:
      - ./instance:/app/instance
      - ./serviceAccountKey.json:/app/serviceAccountKey.json
      - ./types.pkl:/app/types.pkl